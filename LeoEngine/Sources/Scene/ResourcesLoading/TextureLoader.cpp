#include <Scene/ResourcesLoading/TextureLoader.h>

namespace leo {

    std::shared_ptr<const ImageTexture> TextureLoader::loadTexture(const char* fileName)
    {
        return std::shared_ptr<ImageTexture>();
    }

}
